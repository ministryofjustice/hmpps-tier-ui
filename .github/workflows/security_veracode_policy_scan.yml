name: Security veracode policy scan
on:
  workflow_dispatch:
  schedule:
    - cron: "25 4 * * 1" # Every Monday at 04:25 UTC
jobs:
  security-veracode-policy-check:
    permissions:
      contents: read
      actions: read
      security-events: write
    name: Project security veracode policy scan
    uses: ministryofjustice/hmpps-github-actions/.github/workflows/security_veracode_policy_scan.yml@a731b6134c799eb068c00886f87aab0ae2a6efa6 # v2.14.7
    with:
      channel_id: ${{ vars.SECURITY_ALERTS_SLACK_CHANNEL_ID || 'NO_SLACK' }}
    secrets: inherit
